# before starting pipenv, I suggest to run:
# export PIPENV_VENV_IN_PROJECT="enabled"
# this encapsulates the dependencies into the dir of the project,
# same as node_modules.

[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
flask-restful = "*"
google-auth-oauthlib = "*"
google-api-python-client = "*"
pyopenssl = "*"
uwsgi = "*"

[requires]
python_version = "3.7"
